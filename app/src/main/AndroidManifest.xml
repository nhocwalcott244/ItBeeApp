<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lovebarca.itbeeapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".display.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".display.goodJobActivity" />
        <activity android:name=".display.introduceJobActivity" />
        <activity android:name=".display.suggestionJobActivity" />
        <activity android:name=".user.applyJobAvtivity" />
        <activity android:name=".display.topBeeActivity" />
        <activity android:name=".display.topCompanyActivity" />
        <activity android:name=".user.profileActivity" />
        <activity android:name=".display.friendActivity" />
        <activity android:name=".company.createCompanyActivity" />
        <activity android:name=".company.createJobActivity" />
        <activity android:name=".company.companyActivity" />
        <activity android:name=".user.registerActivity" />
        <activity
            android:name=".user.LoginActivity"
            android:label="@string/title_activity_login"></activity>
    </application>

</manifest>